---
title: "#1 Attention explained"
icon: "rocket-launch"
description: "Attention mechanism is so central to all generative models today. Let's talk about how it works!"
---

<div className="sm:grid-cols-4" style={{ display: "list", gap: "4px" }}>
  {Array.from({ length: 20 }, (_, i) => i + 1).map((idx) =>
    idx !== 12 ? (
      <img
        style={{ height: 500 }}
        src={`https://fly.storage.tigris.dev/ai-cartoons/ai-explained-1-attention/${idx}.png`}
      />
    ) : (
      ""
    )
  )}
</div>
